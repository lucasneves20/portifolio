<html lang="pt-br" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Lucas Neves | Portfólio</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-[#120f13] text-[#eed] font-['Inter',sans-serif] min-h-screen flex flex-col overflow-x-hidden"
  >
    <header
      class="w-full border-b border-zinc-800/50 backdrop-blur-md sticky top-0 z-100"
    >
      <div
        class="max-w-7xl mx-auto px-4 sm:px-8 h-20 flex justify-between items-center"
      >
        <h1
          class="text-lg sm:text-xl font-black tracking-tighter hover:text-orange-500 transition-colors cursor-pointer"
        >
          Hello Recruiters
        </h1>

        <nav class="hidden md:flex items-center gap-6 lg:gap-10">
          <div
            class="flex items-center gap-2 border-r border-zinc-800 pr-6 mr-2"
          >
            <span class="relative flex h-2 w-2">
              <span
                class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"
              ></span>
              <span
                class="relative inline-flex rounded-full h-2 w-2 bg-green-500"
              ></span>
            </span>
            <h3
              class="text-[10px] lg:text-xs font-bold text-zinc-500 uppercase tracking-[0.2em] whitespace-nowrap"
            >
              Lucas N. M. Sampaio
            </h3>
          </div>

          <ul class="flex gap-2">
            {
              ["Main", "About", "Contact"].map((item) => (
                <li>
                  <a
                    href={`#${item.toLowerCase()}`}
                    class="px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200 hover:bg-[#eeeed0] hover:text-[#120f13]"
                  >
                    {item}
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>

        <button
          id="menu-toggle"
          class="md:hidden p-2 text-zinc-400 hover:text-white transition-colors"
          aria-label="Abrir Menu"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-7 w-7"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            id="menu-icon"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16m-7 6h7"></path>
          </svg>
        </button>
      </div>

      <nav
        id="mobile-menu"
        class="hidden md:hidden absolute top-20 left-0 w-full bg-[#120f13] border-b border-zinc-800 px-6 py-8 animate-in fade-in slide-in-from-top-4 duration-300"
      >
        <ul class="flex flex-col gap-6">
          <li class="pb-4 border-b border-zinc-900">
            <span
              class="text-[10px] font-bold text-zinc-600 uppercase tracking-widest block mb-1"
              >Status</span
            >
            <span
              class="text-sm text-green-500 font-medium italic underline underline-offset-4"
              >Available for projects</span
            >
          </li>
          {
            ["Main", "About", "Contact"].map((item) => (
              <li>
                <a
                  href={`#${item.toLowerCase()}`}
                  class="mobile-link text-2xl font-bold hover:text-orange-500 transition-colors"
                >
                  {item}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>
    </header>

    <main class="grow w-full max-w-7xl mx-auto px-4 sm:px-8 py-10">
      <slot />
    </main>

    <footer class="py-12 border-t border-zinc-900/50 mt-20 text-center">
      <p class="text-zinc-600 text-[10px] uppercase tracking-[0.3em]">
        Built with Astro & ❤️ by <span class="text-zinc-400">Lucas Sampaio</span
        >
      </p>
    </footer></body
  >
</html>
